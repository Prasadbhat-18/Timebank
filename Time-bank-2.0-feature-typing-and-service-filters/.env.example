# ===========================================
# TimeBank Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control

# ===========================================
# Firebase Configuration (Required)
# ===========================================
# Get these from Firebase Console > Project Settings > General > Your apps
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id_here
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
VITE_FIREBASE_APP_ID=your_firebase_app_id_here

# ===========================================
# Cloudinary Configuration (Required for Image Uploads)
# ===========================================
# Get these from Cloudinary Dashboard > Settings > Upload
VITE_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_upload_preset_name

# ===========================================
# Server Configuration (Optional)
# ===========================================
# Custom server URL for API calls (defaults to localhost:4000)
VITE_SERVER_URL=http://localhost:4000

# ===========================================
# Server-Side Environment Variables
# ===========================================
# These are used by the Node.js server (server/index.ts)
# Set these in your deployment environment (Netlify, Vercel, etc.)

# OTP Server Configuration
OTP_SERVER_PORT=4000
OTP_EMAIL_FROM=no-reply@timebank.local

# SMTP Configuration for Email OTP
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# AI Service Configuration (Groq API)
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-70b-versatile

# ===========================================
# Development Notes
# ===========================================
# 1. Firebase: Enable Authentication (Email/Password) and Firestore Database
# 2. Cloudinary: Create unsigned upload preset for public uploads
# 3. Groq: Sign up at https://console.groq.com/ for AI features
# 4. SMTP: Use services like SendGrid, Mailgun, or Gmail SMTP
# 5. Restart dev server after updating .env.local
# 6. For production, set these variables in your hosting platform
